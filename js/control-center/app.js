import { state, els, escapeHtml, clean, setStatus, appPath, api, openDialog, closeDialog, dialogMessage, field, textarea, value, operationId, activeCases, backlogCases, reviewGroup, allReviewCases } from './shared.js';
import { configureReview, renderReview } from './review.js';
import { renderManage } from './manage.js';
import { renderDevelopment } from './development.js';
async function initializeSharedSession(){return api('/api/control-center/auth.php',{method:'POST',body:JSON.stringify({password:state.password}),timeoutMs:12000});}
async function logout(callServer=true){if(callServer){try{await fetch(appPath('/api/control-center/auth.php'),{method:'DELETE',cache:'no-store'});}catch(_){}}state.password='';sessionStorage.removeItem('be_cc_password');els.content.hidden=true;els.auth.hidden=false;closeDialog();}
function compactSummary(title,count,detail,view,label){return `<article class="cc-summary-card"><div><span class="cc-summary-label">${escapeHtml(title)}</span><strong>${escapeHtml(count)}</strong><p>${escapeHtml(detail)}</p></div>${view?`<button class="cc-button cc-button--primary" data-go-view="${view}">${escapeHtml(label)}</button>`:''}</article>`;}
function renderOverview(){const reviews=allReviewCases(),backlog=backlogCases(),urgent=reviews.filter(item=>item.bucket==='now'||item.overdue||item.state==='blocked'),groups=reviews.reduce((result,item)=>{const key=reviewGroup(item);result[key]=(result[key]||0)+1;return result;},{}),labels={new_content:'Neue Inhalte',quality:'Qualität',provider:'Anbieter',approvals:'Freigaben',system:'System',other:'Sonstige'},groupText=Object.entries(groups).map(([key,count])=>`${count} ${labels[key]||'Sonstige'}`).join(' · ')||'Keine offenen Prüfungen',quality=state.development?.content_quality||{},process=state.development?.automation?.process_health||{},developmentText=`${quality.scope_label||'Aktive Eventbasis'} ${quality.coverage_percent??0} % vollständig · ${['attention','unknown'].includes(process.status)?'Prozessprüfung erforderlich':'Prozesse ohne bekannten Fehler'}`,source=state.overview?.sync?.growth_backlog||{},backlogText=source.status==='error'?'Growth-Backlog konnte nicht geladen werden':backlog.length?'Kanonischer Growth-Backlog':'Kanonischer Growth-Backlog ist leer';els.view.innerHTML=`<section class="cc-overview-grid">${compactSummary('Jetzt erforderlich',urgent.length,urgent.length?urgent.slice(0,2).map(item=>item.title).join(' · '):'Aktuell ist nichts dringend.',urgent.length?'review':'','Jetzt prüfen')}${compactSummary('Zu prüfen',reviews.length,groupText,reviews.length?'review':'','Prüfen öffnen')}${compactSummary('Backlog',backlog.length,backlogText,'backlog','Backlog öffnen')}<article class="cc-summary-card cc-summary-card--quiet"><div><span class="cc-summary-label">Entwicklung</span><h2>${escapeHtml(state.development?.operator_status||state.development?.summary?.label||'Projektstatus')}</h2><p>${escapeHtml(developmentText)}</p></div><button class="cc-button cc-button--secondary" data-go-view="development">Entwicklung öffnen</button></article></section>`;document.querySelectorAll('[data-go-view]').forEach(button=>button.addEventListener('click',()=>setView(button.dataset.goView)));}
function backlogActions(item){const actions=[];if(item.primary_action&&!['convert_to_task','details'].includes(item.primary_action.key))actions.push(item.primary_action);for(const action of item.secondary_actions||[])if(!['convert_to_task','details'].includes(action.key))actions.push(action);const seen=new Set();return actions.filter(action=>action?.key&&!seen.has(action.key)&&seen.add(action.key));}
function backlogRow(item){const actions=backlogActions(item),summary=clean(item.next_action||item.reason).slice(0,150);return `<details class="cc-labor-row" data-case-id="${escapeHtml(item.id)}"><summary><span><b>${escapeHtml(item.title)}</b><small>Backlog${item.priority?` · ${escapeHtml(item.priority)}`:''}</small></span><span class="cc-labor-summary">${escapeHtml(summary)}</span></summary><div class="cc-labor-detail">${item.reason?`<p>${escapeHtml(item.reason)}</p>`:''}${item.next_action&&item.next_action!==item.reason?`<p><strong>Nächster Schritt:</strong> ${escapeHtml(item.next_action)}</p>`:''}<div class="cc-actions">${actions.map(action=>`<button class="cc-button ${action.destructive?'cc-button--danger':action.key==='complete'?'cc-button--primary':'cc-button--secondary'}" data-backlog-action="${escapeHtml(action.key)}">${escapeHtml(action.label)}</button>`).join('')}</div></div></details>`;}
function sourceBanner(items){const source=state.overview?.sync?.growth_backlog||{};if(source.status==='error')return `<div class="cc-notice cc-notice--attention"><strong>Quelle konnte nicht geladen werden.</strong><br>${escapeHtml(source.message||'Der Growth-Backlog ist derzeit nicht erreichbar.')}</div>`;if(source.status==='ok')return `<div class="cc-source-state cc-source-state--ok"><strong>Erfolgreich geladen · ${items.length} offen</strong><span>${escapeHtml(source.label||'Growth-Backlog')} · ${escapeHtml(source.seen??0)} gesehen · ${escapeHtml(source.upserted??0)} synchronisiert</span></div>`;return '<div class="cc-source-state"><strong>Quellenstatus noch nicht bestätigt</strong><span>Übersicht aktualisieren, um den kanonischen Growth-Backlog zu synchronisieren.</span></div>';}
function renderBacklog(){const items=backlogCases();els.view.innerHTML=`<div class="cc-toolbar cc-toolbar--backlog"><div><span class="cc-kicker">Geplante Entwicklung</span><strong>${items.length} offen</strong></div><button class="cc-button cc-button--primary" id="cc-new-backlog">+ Neu</button></div>${sourceBanner(items)}<div class="cc-labor-list">${items.length?items.map(backlogRow).join(''):'<div class="cc-empty cc-empty--large">Der kanonische Growth-Backlog wurde erfolgreich geladen und enthält keinen offenen Punkt.</div>'}</div>`;document.querySelector('#cc-new-backlog')?.addEventListener('click',openBacklogDialog);document.querySelectorAll('[data-backlog-action]').forEach(button=>button.addEventListener('click',()=>{const item=state.cases.find(candidate=>candidate.id===button.closest('[data-case-id]')?.dataset.caseId);handleBacklog(item,button.dataset.backlogAction);}));}
async function submitBacklog(item,action,payload,id=operationId()){return api('/api/control-center/action.php',{method:'POST',body:JSON.stringify({case_id:item.id,action,operation_id:id,payload})});}
function openBacklogDialog(){openDialog(`<h2>Backlogpunkt anlegen</h2><div id="cc-dialog-message"></div><div class="cc-stack"><label class="cc-field"><span>Titel</span><input id="cc-backlog-title" required></label>${textarea('cc-backlog-note','Beschreibung / Nutzen','')}<label class="cc-field"><span>Priorität</span><select id="cc-backlog-priority"><option value="hoch">Hoch</option><option value="mittel" selected>Mittel</option><option value="niedrig">Niedrig</option></select></label><button type="button" class="cc-button cc-button--primary" id="cc-backlog-save">Speichern</button></div>`);document.querySelector('#cc-backlog-save')?.addEventListener('click',async event=>{const title=value('#cc-backlog-title');if(!title)return dialogMessage('Titel fehlt.');event.currentTarget.disabled=true;try{await api('/api/growth-backlog/create.php',{method:'POST',body:JSON.stringify({title,note:value('#cc-backlog-note'),priority:value('#cc-backlog-priority')})});closeDialog();await load();setView('backlog');}catch(error){event.currentTarget.disabled=false;dialogMessage(error.message);}});}
async function handleBacklog(item,action){if(!item)return;if(action==='edit_source'){openDialog(`<h2>Backlog bearbeiten</h2><div id="cc-dialog-message"></div><div class="cc-stack">${field('bl-title','Titel',item.title)}${textarea('bl-description','Beschreibung',item.reason)}<label class="cc-field"><span>Priorität</span><select id="bl-priority"><option value="hoch" ${item.priority==='high'?'selected':''}>Hoch</option><option value="mittel" ${item.priority==='normal'?'selected':''}>Mittel</option><option value="niedrig" ${item.priority==='low'?'selected':''}>Niedrig</option></select></label><button type="button" class="cc-button cc-button--primary" id="bl-save">Speichern</button></div>`);const id=operationId();document.querySelector('#bl-save')?.addEventListener('click',async event=>{event.currentTarget.disabled=true;try{await submitBacklog(item,'edit_source',{title:value('#bl-title'),description:value('#bl-description'),priority:value('#bl-priority')},id);closeDialog();await load();setView('backlog');setStatus('Backlogpunkt gespeichert.','success');}catch(error){event.currentTarget.disabled=false;dialogMessage(error.message);}});return;}const payload=action==='reject'?{decision_class:'rejected_low_value',decision_note:'Backlogpunkt über die Steuerzentrale verworfen.'}:{};setStatus('Backlog wird aktualisiert …');try{await submitBacklog(item,action,payload);await load();setView('backlog');setStatus('Gespeichert.','success');}catch(error){setStatus(error.message,'attention');}}
function updateBadges(){[[els.badgeReview,allReviewCases().length],[els.badgeBacklog,backlogCases().length]].forEach(([element,count])=>{element.hidden=count===0;element.textContent=count>99?'99+':String(count);});}
export async function load(){setStatus('Daten werden geladen …');try{const[overview,cases,development]=await Promise.all([api('/api/control-center/overview.php'),api('/api/control-center/cases.php'),api('/api/control-center/development.php')]);state.overview=overview;state.cases=cases.items||[];state.development=development;updateBadges();render();setStatus('');}catch(error){if(error.status===401)logout(false);setStatus(error.message,'attention');}}
configureReview({reload:load});
function render(){if(state.view!=='development'||state.developmentMode!=='seo')els.view.classList.remove('cc-view--seo');if(state.view==='overview')renderOverview();else if(state.view==='review')renderReview();else if(state.view==='backlog')renderBacklog();else if(state.view==='manage')renderManage();else if(state.view==='development')renderDevelopment();}
function setView(view){state.view=view;setStatus('');const titles={overview:'Übersicht',review:'Prüfen',backlog:'Backlog',manage:'Verwaltung',development:'Entwicklung'};els.title.textContent=titles[view]||'Steuerzentrale';els.nav.forEach(button=>button.classList.toggle('is-active',button.dataset.view===view));render();window.scrollTo({top:0,behavior:'instant'});}
function openSystemDialog(){const systemCases=allReviewCases().filter(item=>reviewGroup(item)==='system');closeDialog();openDialog(`<h2>Systemstatus</h2><p>${escapeHtml(state.overview?.system?.message||'Keine bekannte Störung mit Auswirkung.')}</p><ul class="cc-system-list"><li><strong>Prüffälle</strong><span>${allReviewCases().length} offen</span></li><li><strong>Systemfälle</strong><span>${systemCases.length} offen</span></li><li><strong>Backlog</strong><span>${backlogCases().length} offen</span></li></ul><details class="cc-disclosure"><summary>Technische Details</summary><div>${escapeHtml(JSON.stringify(state.overview?.sync||{},null,2))}</div></details>`);}
function openHeaderMenu(){openDialog(`<h2>Menü</h2><div class="cc-link-list"><a class="cc-link-card" href="${escapeHtml(appPath('/fuer-veranstalter/dashboard/'))}"><strong>Anbieterbereich</strong><span>Einreichungen und Anbieterwirkung</span></a><button class="cc-link-card" id="cc-system"><strong>Systemstatus</strong><span>Prozesse und Auswirkungen prüfen</span></button><button class="cc-link-card" id="cc-deploy"><strong>Deployment starten</strong><span>Datenbestand neu veröffentlichen</span></button><button class="cc-link-card" id="cc-logout"><strong>Abmelden</strong><span>Sitzungszugang entfernen</span></button></div>`);document.querySelector('#cc-system')?.addEventListener('click',openSystemDialog);document.querySelector('#cc-deploy')?.addEventListener('click',async()=>{closeDialog();setStatus('Deployment wird gestartet …','info');try{const result=await api('/api/control-center/deploy.php',{method:'POST',body:'{}'});setStatus(result.message,'success');}catch(error){setStatus(error.message,'attention');}});document.querySelector('#cc-logout')?.addEventListener('click',()=>logout(true));}
els.authForm.addEventListener('submit',async event=>{event.preventDefault();state.password=clean(els.password.value);setStatus('Zugang wird geprüft …');try{await initializeSharedSession();sessionStorage.setItem('be_cc_password',state.password);els.auth.hidden=true;els.content.hidden=false;await load();}catch(error){state.password='';setStatus(error.message,'attention');}});
els.refresh.addEventListener('click',()=>{state.managementLoaded={events:false,activities:false};state.managementWarnings={events:[],activities:[]};state.development=null;load();});els.menuButton.addEventListener('click',openHeaderMenu);els.nav.forEach(button=>button.addEventListener('click',()=>setView(button.dataset.view)));els.dialogClose.addEventListener('click',closeDialog);els.dialog.addEventListener('click',event=>{if(event.target===els.dialog)closeDialog();});if(state.password){els.auth.hidden=true;els.content.hidden=false;initializeSharedSession().then(load).catch(()=>logout(false));}

name: PR Gate

on:
  pull_request:
    branches: ["staging", "main"]
    types: [opened, synchronize, reopened, ready_for_review]

concurrency:
  group: pr-gate-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: PR Gate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enforce serial branch path
        shell: bash
        env:
          BASE_REF: ${{ github.base_ref }}
          HEAD_REF: ${{ github.head_ref }}
          HEAD_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name }}
          CURRENT_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          test "$HEAD_REPOSITORY" = "$CURRENT_REPOSITORY"
          if [ "$BASE_REF" = "staging" ]; then
            exit 0
          fi
          if [ "$BASE_REF" = "main" ] && [ "$HEAD_REF" = "staging" ]; then
            exit 0
          fi
          echo "Allowed paths: feature branch -> staging, staging -> main."
          exit 1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Validate repository tools
        shell: bash
        run: bash scripts/validate-repo.sh repository

      - name: Validate backend and data contracts
        id: backend
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          bash scripts/validate-repo.sh backend 2>&1 | tee backend-validation.log

      - name: Upload temporary backend diagnostic
        if: steps.backend.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: backend-validation-diagnostic
          path: backend-validation.log
          if-no-files-found: error

      - name: Fail on backend contract error
        if: steps.backend.outcome == 'failure'
        run: exit 1

      - name: Validate frontend contracts
        shell: bash
        run: bash scripts/validate-repo.sh frontend

# === BEGIN FILE: .github/workflows/deploy-strato.yml ===
# Zweck: Vollautomatischer Deploy zu STRATO + automatische Build-Version
# - Keine manuellen Versionsnummern
# - build.json wird automatisch erzeugt
# - CSS/JS erhalten Cache-Busting (?v=<commit>)
# - Deploy per SFTP nach STRATO-apps (verifiziertes Webroot)
# === END FILE HEADER ===

name: Deploy to STRATO via SFTP

on:
  push:
    branches: [ "main" ]

concurrency:
  group: deploy-strato
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # ------------------------------------------------------------
      # Checkout
      # ------------------------------------------------------------
      - name: Checkout
        uses: actions/checkout@v4

      # ------------------------------------------------------------
      # Compute BUILD_ID (Commit SHA, automatisch)
      # ------------------------------------------------------------
      - name: Compute BUILD_ID
        shell: bash
        run: |
          set -e
          BUILD_ID="$(git rev-parse --short=12 HEAD)"
          echo "BUILD_ID=$BUILD_ID" >> "$GITHUB_ENV"

      # ------------------------------------------------------------
      # Prepare deploy folder
      # - build.json erzeugen
      # - index.html mit Cache-Busting patchen
      # ------------------------------------------------------------
      - name: Prepare deploy folder
        shell: bash
        run: |
          set -e

          rm -rf deploy
          mkdir -p deploy

          # Alles Relevante kopieren
          rsync -a \
            --exclude=".git" \
            --exclude=".github" \
            --exclude="deploy" \
            ./ deploy/

          # build.json für Service Worker Versioning
          printf '{ "version": "%s" }\n' "$BUILD_ID" > deploy/build.json

          # Cache-Busting für CSS
          if [ -f deploy/index.html ]; then
            perl -0777 -pi -e 's/href="\/css\/style\.css"/href="\/css\/style.css?v='"$ENV{BUILD_ID}"'"/g' deploy/index.html
            perl -0777 -pi -e 's/src="(\/js\/[^"?]+\.js)"/src="$1?v='"$ENV{BUILD_ID}"'"/g' deploy/index.html
          fi

          echo "---- DEPLOY CONTENTS ----"
          ls -la deploy
          echo "---- build.json ----"
          cat deploy/build.json

      # ------------------------------------------------------------
      # Install lftp
      # ------------------------------------------------------------
      - name: Install lftp
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y lftp

      # ------------------------------------------------------------
      # Deploy via SFTP (STRATO)
      # Webroot ist verifiziert: STRATO-apps
      # ------------------------------------------------------------
      - name: Deploy via SFTP
        shell: bash
        env:
          STRATO_FTP_SERVER: ${{ secrets.STRATO_FTP_SERVER }}
          STRATO_FTP_USERNAME: ${{ secrets.STRATO_FTP_USERNAME }}
          STRATO_FTP_PASSWORD: ${{ secrets.STRATO_FTP_PASSWORD }}
        run: |
          set -e

          lftp -u "$STRATO_FTP_USERNAME","$STRATO_FTP_PASSWORD" \
            sftp://$STRATO_FTP_SERVER \
            -e "
              set cmd:fail-exit yes;
              set sftp:auto-confirm yes;
              set net:timeout 20;
              set net:max-retries 2;

              echo '--- Remote PWD (before) ---';
              pwd;

              echo '--- cd to STRATO-apps (webroot) ---';
              cd STRATO-apps;
              pwd;

              echo '--- Remote listing (before) ---';
              ls -la;

              echo '--- MIRROR UPLOAD ---';
              mirror -R --verbose --only-newer deploy/ .;

              echo '--- Remote listing (after) ---';
              ls -la;

              bye
            "
# === END FILE: .github/workflows/deploy-strato.yml ===

name: Deploy to STRATO via SFTP

on:
  push:
    branches: [ "main" ]

concurrency:
  group: deploy-strato
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute BUILD_ID
        run: |
          BUILD_ID="$(git rev-parse --short=12 HEAD)"
          echo "BUILD_ID=$BUILD_ID" >> "$GITHUB_ENV"

      - name: Prepare deploy folder
        run: |
          rm -rf deploy
          mkdir deploy

          rsync -a \
            --exclude=".git" \
            --exclude=".github" \
            --exclude="deploy" \
            ./ deploy/

          echo "{ \"version\": \"$BUILD_ID\" }" > deploy/build.json

          if [ -f deploy/index.html ]; then
            sed -i "s|/css/style.css|/css/style.css?v=$BUILD_ID|g" deploy/index.html
            sed -i "s|/js/|/js/|g" deploy/index.html
          fi

          ls -la deploy

      - name: Install lftp
        run: |
          sudo apt-get update
          sudo apt-get install -y lftp

      - name: Deploy via SFTP
        env:
          STRATO_FTP_SERVER: ${{ secrets.STRATO_FTP_SERVER }}
          STRATO_FTP_USERNAME: ${{ secrets.STRATO_FTP_USERNAME }}
          STRATO_FTP_PASSWORD: ${{ secrets.STRATO_FTP_PASSWORD }}
        run: |
          lftp -u "$STRATO_FTP_USERNAME","$STRATO_FTP_PASSWORD" sftp://$STRATO_FTP_SERVER <<EOF
          set sftp:auto-confirm yes
          set cmd:fail-exit yes

          pwd
          ls -la

          mirror -R --only-newer deploy/ .

          ls -la
          bye
EOF

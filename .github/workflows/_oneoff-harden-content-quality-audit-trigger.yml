# === DISABLED TEMP FILE: .github/workflows/_oneoff-harden-content-quality-audit-trigger.yml ===
# Zweck:
# - Dokumentiert einen fehlgeschlagenen One-off-Ansatz vom 2026-07-07.
# - Nicht erneut verwenden.
#
# Warum deaktiviert:
# - workflow_dispatch-Button ist fuer Workflows, die nur auf staging liegen, nicht verlaesslich sichtbar,
#   weil GitHub den manuell ausloesbaren Workflow auf dem Default-Branch erwartet.
# - Ein GitHub-Actions-Run mit GITHUB_TOKEN/contents:write darf in diesem Repo keine Dateien
#   unter .github/workflows/ veraendern; der Push wurde mit fehlender workflows-Permission abgelehnt.
#
# Regel:
# - Workflow-Dateien nicht durch selbstmodifizierende One-off-Workflows aendern.
# - Stattdessen GitHub-Connector, lokale Arbeitskopie/Codespace oder bewusstes Patch-Paket nutzen.

name: Disabled One-off Harden Content Quality Audit Trigger

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "This one-off workflow is intentionally disabled and must not be used."
          echo "See docs/github-actions-trigger-policy.md."
# === END DISABLED TEMP FILE ===

- name: Run event source proof
  run: |
    python -m pip install --upgrade pip
    pip install playwright
    python -m playwright install --with-deps chromium
    python scripts/_proof_event_source_extraction.py
